/* =====================================================================
 *  PGY 訓練課程表 — 前端設定檔
 *  串接 Google Sheets 時，只需要改這一個檔案。
 * ===================================================================== */
window.PGY_CONFIG = {

  /* ------------------------------------------------------------------
   * 1) GAS Web App 網址
   *    部署 gas/Code.gs 之後，把「網頁應用程式」網址貼在這裡。
   *    格式：https://script.google.com/macros/s/AKfycb.../exec
   *    留空字串 = 使用 assets/sample-data.js 的離線範例資料。
   * ------------------------------------------------------------------ */
  GAS_WEB_APP_URL: 'https://script.google.com/macros/s/AKfycbwP08wG81orF4itPVpavCXh_ZyMghU1ZUXbWFma8ySCJrQBTWJKH6QxcykvJjps3xJhPA/exec',

  /* 預設要載入的工作表名稱（留空 = 用試算表的第一個工作表） */
  DEFAULT_SHEET: '',

  /* 呼叫 GAS 的方式：
   *   'auto'  建議 — 先用 fetch，被 CORS 擋住時自動改用 JSONP
   *   'fetch' 只用 fetch
   *   'jsonp' 只用 JSONP（最不會被瀏覽器擋，但只能讀取）            */
  TRANSPORT: 'auto',

  /* 自動重新整理間隔（毫秒）。0 = 關閉。例：5 分鐘 = 300000 */
  AUTO_REFRESH_MS: 0,

  /* 網頁標題 */
  TITLE: 'PGY 訓練課程表',
  SUBTITLE: '畢業後一般醫學訓練　年度輪訓月曆',

  /* ------------------------------------------------------------------
   * 2) 顏色來源
   *    'category' 一律依「科別」上色 —— 開頭是「內」的一律黃色，
   *               不管後面寫 內(Y2不分) 還是 內(外)。同科別顏色一定一致。
   *    'sheets'   以 Google Sheets 的儲存格底色為主，
   *               只有在該格沒有底色時才用下面的預設色票。
   * ------------------------------------------------------------------ */
  COLOR_SOURCE: 'category',

  /* ------------------------------------------------------------------
   * 3) 科別色票
   * ------------------------------------------------------------------ */
  CATEGORY_COLORS: {
    '內科':   '#FFC000',
    '外科':   '#92D050',
    '急診':   '#00B0F0',
    '兒科':   '#9D6201',
    '婦產':   '#FFCCCC',
    '社區':   '#A9D9CE',
    '選修':   '#C8B9E0',
    '其他':   '#D9D9D9'
  },

  /* 值 → 科別分類規則
   *
   * 【只看開頭】依下列順序比對，開頭符合就歸類，後面寫什麼都不影響：
   *     內、內(Y2不分)、內(外)、內科   → 內科
   *     急-內、急-外                  → 急診（開頭是「急」）
   *     外(整外)、外-內               → 外科
   *     選-內、選-眼科                → 選修
   *   都不符合 → 其他
   *
   * 要新增科別：在 CATEGORY_COLORS 加一組顏色，再在這裡加一條規則即可。
   * 注意順序：排在前面的先比對。 */
  CATEGORY_RULES: [
    { prefix: '內', name: '內科' },
    { prefix: '外', name: '外科' },
    { prefix: '急', name: '急診' },
    { prefix: '兒', name: '兒科' },
    { prefix: '婦', name: '婦產' },
    { prefix: '社', name: '社區' },
    { prefix: '選', name: '選修' }
  ]
};
